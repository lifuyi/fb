# Campus Connect - Web Frontend (Phase 1)

面向黑龙江省大学生的社交与服务平台 - Web 前端应用

## 🚀 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **状态管理**: Zustand + TanStack Query (React Query)
- **表单**: React Hook Form + Zod
- **UI组件**: 自定义组件库
- **主题**: next-themes (支持亮色/暗色模式)
- **图标**: Lucide React

## 📦 功能特性 (Phase 1)

### ✅ 已实现
- **用户认证**: 邮箱登录、手机验证码登录
- **动态发布**: 发布文字、图片帖子，点赞、评论功能
- **群组管理**: 创建群组、加入/退出群组、群组动态
- **响应式布局**: Facebook/LinkedIn 风格的三栏布局
- **暗色模式**: 支持亮色/暗色主题切换
- **无限滚动**: 帖子列表支持无限加载
- **SEO优化**: 服务端渲染，利于搜索引擎收录

### 🚧 待开发
- **微信SSO登录**: 等待微信开放平台凭证
- **好友管理**: 添加好友、好友列表
- **兼职市场**: 浏览、发布兼职信息
- **房屋租赁**: 租房信息浏览和发布
- **餐厅点评**: 餐厅评价和推荐

## 🎨 设计特点

### 配色方案
- **主色调**: 深红色 (#8B0000)
- **辅助色**: 鲜红色 (#DC143C)
- **亮色模式**: 白色背景 (#FFFFFF)
- **暗色模式**: 深灰背景 (#1A1A1A)

### 布局结构
```
┌─────────────────────────────────────────────────┐
│                   Header                         │
├──────────┬──────────────────────┬───────────────┤
│          │                      │               │
│  Left    │    Main Content      │  Right        │
│  Sidebar │    (Feed/Groups)     │  Sidebar      │
│          │                      │  (Suggestions)│
│          │                      │               │
└──────────┴──────────────────────┴───────────────┘
```

## 🛠️ 开发指南

### 环境要求
- Node.js 18+ 
- npm 或 yarn 或 pnpm

### 安装依赖
```bash
cd frontend
npm install
```

### 环境变量配置
创建 `.env.local` 文件：
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_NAME=Campus Connect
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 启动开发服务器
```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本
```bash
npm run build
npm start
```

## 📁 项目结构

```
frontend/
├── app/                    # Next.js 应用路由
│   ├── feed/              # 动态页面
│   ├── groups/            # 群组页面
│   ├── login/             # 登录页面
│   ├── gigs/              # 兼职市场 (待开发)
│   ├── housing/           # 房屋租赁 (待开发)
│   ├── restaurants/       # 餐厅点评 (待开发)
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页 (重定向到 feed)
│   ├── providers.tsx      # 全局 Provider
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── layout/           # 布局组件
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── RightSidebar.tsx
│   │   └── MainLayout.tsx
│   ├── post/             # 帖子相关组件
│   │   ├── PostCard.tsx
│   │   ├── CreatePost.tsx
│   │   └── PostList.tsx
│   ├── group/            # 群组相关组件
│   │   ├── GroupCard.tsx
│   │   ├── GroupList.tsx
│   │   └── CreateGroupModal.tsx
│   └── ui/               # 基础 UI 组件
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Card.tsx
│       ├── Avatar.tsx
│       └── Modal.tsx
├── lib/                   # 工具库
│   ├── api.ts            # API 客户端
│   ├── types.ts          # TypeScript 类型定义
│   ├── utils.ts          # 工具函数
│   ├── hooks/            # 自定义 Hooks
│   │   ├── useAuth.ts
│   │   ├── usePosts.ts
│   │   └── useGroups.ts
│   ├── services/         # API 服务
│   │   ├── auth.service.ts
│   │   ├── post.service.ts
│   │   └── group.service.ts
│   └── store/            # Zustand 状态管理
│       └── auth.store.ts
├── public/               # 静态资源
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## 🔌 API 集成

### 后端 API 端点
所有 API 请求都通过 `lib/api.ts` 中的 `apiClient` 发送，自动处理：
- 认证 Token (Bearer Token)
- 错误处理和重试
- 401 自动跳转登录

### 主要 API 服务
- **authService**: 用户认证相关
- **postService**: 帖子管理
- **groupService**: 群组管理

## 🚀 部署指南 (阿里云)

### 方式一：使用 PM2 部署
```bash
# 构建
npm run build

# 使用 PM2 启动
pm2 start npm --name "campus-connect-web" -- start
pm2 save
pm2 startup
```

### 方式二：使用 Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📝 开发规范

### 组件命名
- React 组件使用 PascalCase: `PostCard.tsx`
- 文件夹使用 kebab-case: `post-list/`
- 工具函数使用 camelCase: `formatDate()`

### 样式规范
- 优先使用 Tailwind CSS 工具类
- 自定义样式放在 `globals.css`
- 使用 `cn()` 工具函数合并类名

### 状态管理
- 服务器状态: TanStack Query (缓存、自动刷新)
- 客户端状态: Zustand (轻量级)
- 表单状态: React Hook Form

## 🐛 已知问题

1. **图片上传**: 当前仅支持预览，需集成阿里云 OSS
2. **微信登录**: 需要微信开放平台凭证
3. **好友管理**: 后端 API 待开发
4. **通知系统**: 待实现实时推送

## 📞 联系方式

如有问题，请联系开发团队。

## 📄 许可证

© 2024 Campus Connect. All rights reserved.
